# Disable lifecycle scripts by default. Required project scripts are invoked
# explicitly in development and CI.
ignore-scripts=true

# Delay newly published dependencies long enough for ecosystem review.
min-release-age=14
